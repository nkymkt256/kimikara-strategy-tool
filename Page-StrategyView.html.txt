<!-- Page-StrategyView.html -->
<section class="strategy-view-container">
  <h2>レッスン当日作戦表示・振り返り</h2>

  <div class="strategy-container">
    <!-- 動的に生成される生徒表示・振り返り入力エリア -->
    <div id="students-view-area"></div>

    <!-- 保存ボタン -->
    <div class="action-buttons">
      <button id="save-feedback-btn" onclick="saveFeedbackAndGenerate()" class="primary-btn" disabled>
        振り返りを保存してスライド生成
      </button>
    </div>

    <div id="generation-status"></div>
  </div>
</section>

<template id="student-view-template">
  <div class="student-form" data-student-id="">
    <h3>生徒: <span class="student-name-display"></span></h3>

    <div class="student-info">
      <!-- 作戦の表示 -->
      <div class="strategy-display-section">
        <h4 style="color: #2c3e50; margin-bottom: 10px;">本日の作戦</h4>
        <div class="student-strategies-container"></div>
      </div>

      <!-- レッスン全体の振り返り入力 -->
      <div class="lesson-feedback-section" style="margin-top: 20px; padding-top: 20px; border-top: 2px solid #e0e0e0;">
        <h4 style="color: #2c3e50; margin-bottom: 15px;">レッスンの振り返り</h4>

        <div class="form-group">
          <label>今回のレッスンの内容:</label>
          <textarea
            class="lesson-content-input"
            rows="4"
            placeholder="今回のレッスンで何をやったかを記入してください..."
          ></textarea>
        </div>

        <div class="form-group">
          <label>ふりかえり:</label>
          <textarea
            class="lesson-feedback-input"
            rows="5"
            placeholder="レッスンの振り返りを記入してください..."
          ></textarea>
        </div>

        <div class="form-group">
          <label>次回のレッスンでやること:</label>
          <textarea
            class="lesson-next-input"
            rows="4"
            placeholder="次回のレッスンで取り組むことを記入してください..."
          ></textarea>
        </div>
      </div>
    </div>
  </div>
</template>
