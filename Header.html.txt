<!-- Header.html (ナビゲーション) -->
<style>
  /* ナビゲーションバーのスタイル（背景無し、フラット） */
  nav {
    background: transparent;
    padding: 12px 0;
    margin: 0 0 30px 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* コンテナ内でのロゴ */
  .nav-logo {
    font-weight: 800;
    font-size: 1.1rem;
    color: #2c3e50;
    padding-left: 8px;
  }

  nav ul {
    list-style: none;
    display: flex;
    gap: 8px;
    padding: 0;
    margin: 0;
    align-items: center;
  }

  nav li {
    flex: none;
  }

  nav a {
    display: block;
    padding: 12px 16px;
    color: #2c3e50;
    text-decoration: none;
    text-align: center;
    font-weight: 600;
    transition: all 0.15s;
    border-radius: 6px;
  }

  nav a:hover {
    background: rgba(0,0,0,0.04);
    color: #1a2b3c;
  }

  @media (max-width: 768px) {
    nav {
      flex-direction: column;
      align-items: stretch;
    }

    nav ul {
      justify-content: space-around;
      margin-top: 8px;
    }
  }
</style>

<nav>
  <div class="nav-logo">キミカララボ レッスン作戦 ツール</div>
  <ul>
    <li><a href="#" data-page="Page-StrategyInput" onclick="handleNavClick(event)">作戦入力</a></li>
    <li><a href="#" data-page="Page-StrategyView" onclick="handleNavClick(event)">作戦表示</a></li>
    <li><a href="#" data-page="Page-GoalManagement" onclick="handleNavClick(event)">目標管理</a></li>
  </ul>
</nav>
